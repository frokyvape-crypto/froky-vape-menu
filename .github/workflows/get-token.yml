name: Cafe24 get-token disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is disabled because Cafe24 authorization codes expire quickly and are single-use."
          echo "Use a fresh authorization code outside the repository, then store only the refresh token as a secret."
