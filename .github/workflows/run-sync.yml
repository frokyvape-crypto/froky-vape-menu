name: Cafe24 run-sync disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow no longer consumes Cafe24 refresh tokens."
          echo "Use the 'Cafe24 product sync' workflow instead."
