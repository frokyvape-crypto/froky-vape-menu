name: Cafe24 token-now disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is disabled because it could expose or consume Cafe24 tokens."
          echo "Use the 'Cafe24 product sync' workflow after secrets are configured."
