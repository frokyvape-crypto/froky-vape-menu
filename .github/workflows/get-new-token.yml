name: Cafe24 one-time token exchange disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is disabled because authorization codes are single-use and must not be committed."
          echo "Generate a fresh Cafe24 authorization code manually, then update CAFE24_REFRESH_TOKEN in repository secrets."
